<div>
  <h1 class="font-bold text-4xl"><%= @lodging.name %> Calendar</h1>
</div>

<% @calendars.each do |calendar| %>
  <%= calendar.name %>
  <%= "Duration #{calendar.start_date} #{calendar.end_date}" %>
  <%= "Status #{calendar.status}" %>
<% end %>
